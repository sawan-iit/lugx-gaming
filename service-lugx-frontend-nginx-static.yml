apiVersion: v1
kind: Service
metadata:
  name: lugx-frontend-nginx-service
spec:
  type: NodePort
  selector:
    app: lugx-frontend-nginx
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
